#include <iostream>
#include <algorithm>
using namespace std;

#define maxn 1010
#define int long long     

int n,m,top;
char ch[maxn][maxn];
int h[maxn],l[maxn],r[maxn],st[maxn];
int ans = 0;

signed main(){
    cin>>n>>m;
    for(int i = 1;i <= n;i++){
       for(int j = 1;j <= m;j++){
           cin>>ch[i][j]; 
       }
    }
    for()
    
}