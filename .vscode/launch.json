{
    "version": "0.2.0",
    "configurations": [
        {
            "preLaunchTask": "C/C++: clang++ 生成活动文件",
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",  // 修改这里
            "args": [],
            "cwd": "${fileDirname}"  // 修改这里
        }
    ]
}